{
  "status": "{{ status | default('ready') }}",
  "currency": "USD",
  {#- name = <provider>/<bare> per the platform service-naming convention
      (unitysvc PR #1196 / issue #1138). #}
  "name": "{{ provider_name }}/{{ offering_name }}",
{%- if list_price %}
{#- Price-cell copy follows the marketplace description grammar
    (unitysvc/unitysvc#1886, PR #1887):

        <amount> [ ~<intent> <PILL> ] [ | <hover note> ]

    The cell used to read "Free (BYOK)" — one flat string, with the
    parenthetical doing badge work the parser cannot see, and the upstream
    rate reachable only by opening the service page. Now the amount is Free,
    *why* it is free rides the badge slot as a pill (a bare `~` selects the
    default intent — a saving — which is the green fill; `~deal` / `~coupon` /
    `~info` are the other registers), and Cohere's reference rate rides the
    `|` note. That note is taken first and consumed to end-of-string, which
    makes everything in it inert — a rate card full of "/" and "per" is safe
    there and ONLY there.

    Models with no rate in the upstream data render "Free ~ BYOK" alone. That
    is correct, not incomplete: there is no number to state.

    Composed here rather than written into the param file because
    scripts/update_params.py rebuilds list_price on the nightly populate run
    and would drop a hand-edited description within a day. It overrides only
    `description`; `price`/`type` are carried through untouched, and
    `payout_price` (seller-facing, same dict upstream) is not touched at all. #}
{%- set _rate = (' | usage is billed by ' ~ provider_display_name ~ ' directly at ' ~ pricing_note) if pricing_note else '' %}
  "list_price": {{ dict(list_price, description='Free ~ BYOK' ~ _rate) | tojson }},
{%- endif %}
  "user_access_interfaces": {
    "provider_api": {
      "access_method": "http",
      "base_url": "${API_GATEWAY_BASE_URL}/{{ provider_name }}",
      "routing_key": {"model": "{{ offering_name }}"}
    }
  },
  "documents": {
    "How to use this model": {
      "$doc_preset": "llm_description"
    }
{#- Transcription models hit Cohere's compat-layer ``/audio/transcriptions``
    surface, not ``/chat/completions``.  Embedding models hit ``/embeddings``.
    Dispatch by service_type / is_transcription so each gets the
    correct-shape preset; sending a chat payload to a transcription model
    returns 400 from upstream. Executed docs carry meta.sleep_after_test (R7). -#}
{%- if is_transcription %},
    "Python code example": {
      "$doc_preset": {
        "name": "llm_code_example_transcription_requests",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "JavaScript code example": {
      "$doc_preset": {
        "name": "llm_code_example_transcription_javascript",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "cURL code example": {
      "$doc_preset": {
        "name": "llm_code_example_transcription_shell",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "Connectivity test": {
      "$doc_preset": {
        "name": "llm_connectivity_transcription",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    }
{%- elif is_image_embedding %},
{#- Image-embedding models hit Cohere's NATIVE ``/v2/embed`` with
    ``input_type=image`` — the OpenAI-compat ``/embeddings`` rejects them
    ("does not support text embeddings").  The ``llm_code_example_embed_image_*``
    presets in unitysvc-data already use the Cohere-native shape
    (``images``, ``input_type``, ``embedding_types``); we just point
    ``version_prefix`` at ``/v2``.  No matching connectivity_test preset
    yet — the cURL code example doubles as the smoke probe via inline. -#}
    "Python code example": {
      "$doc_preset": {
        "name": "llm_code_example_embed_image_requests",
        "version_prefix": "/v2",
        "meta": {"sleep_after_test": 5}
      }
    },
    "JavaScript code example": {
      "$doc_preset": {
        "name": "llm_code_example_embed_image_javascript",
        "version_prefix": "/v2",
        "meta": {"sleep_after_test": 5}
      }
    },
    "cURL code example": {
      "$doc_preset": {
        "name": "llm_code_example_embed_image_shell",
        "version_prefix": "/v2",
        "meta": {"sleep_after_test": 5}
      }
    },
    "Connectivity test": {
      "$doc_preset": {
        "name": "llm_connectivity_embed_image",
        "version_prefix": "/v2",
        "meta": {"sleep_after_test": 5}
      }
    }
{%- elif service_type == "embedding" %},
    "Python code example": {
      "$doc_preset": {
        "name": "llm_code_example_embed_requests",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "JavaScript code example": {
      "$doc_preset": {
        "name": "llm_code_example_embed_javascript",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "cURL code example": {
      "$doc_preset": {
        "name": "llm_code_example_embed_shell",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "Connectivity test": {
      "$doc_preset": {
        "name": "llm_connectivity_embed",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    }
{%- else %},
    "Python code example (OpenAI SDK)": {
      "$doc_preset": {
        "name": "llm_code_example_openai",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "Python code example (Cohere SDK)": {
      "$doc_preset": {"name": "llm_code_example_cohere", "meta": {"sleep_after_test": 5}}
    },
    "JavaScript code example": {
      "$doc_preset": {
        "name": "llm_code_example_javascript",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "cURL code example": {
      "$doc_preset": {
        "name": "llm_code_example_shell",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    {#- Anthropic-dialect examples (R9): the upstream speaks OpenAI (Cohere's
        Compatibility API), so specs run-tests (local_testing) calls it in
        OpenAI form, while the gateway path speaks Anthropic and exercises the
        llm_translator. -#}
    "Python code example (Anthropic SDK)": {
      "$doc_preset": {
        "name": "llm_code_example_anthropic_to_openai_sdk",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "cURL code example (Anthropic style)": {
      "$doc_preset": {
        "name": "llm_code_example_anthropic_to_openai_shell",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "Connectivity test": {
      "$doc_preset": {
        "name": "llm_connectivity",
        "version_prefix": "/compatibility/v1",
        "meta": {"sleep_after_test": 5}
      }
    },
    "Default request body": {
      "$doc_preset": "llm_request_template"
    }
{%- endif %}
  }
}
