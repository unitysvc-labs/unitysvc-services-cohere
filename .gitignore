
# Test artifacts downloaded by connectivity / code-example scripts
*.flac
*.jpg
*.jpeg
*.png
*.wav
*.mp3

# Per-service test artifacts from `usvc_seller data run-tests`
# Test outputs. Path-anchored patterns were left behind by the move to the
# services/specs/ layout, so artifacts were no longer ignored; match by
# extension instead so the layout cannot strand them again.
*.out
*.err
*.status
failed*
